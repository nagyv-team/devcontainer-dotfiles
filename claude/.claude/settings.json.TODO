TODO for task 5: Modify settings.json to add telegram_notify.py to hooks

1. Add Stop hook configuration:
   "Stop": [
     {
       "hooks": [
         {
           "type": "command",
           "command": "${HOME}/.venv_dotfiles/bin/python ${HOME}/.claude/scripts/telegram_notify.py"
         }
       ]
     }
   ]

2. Update Notification hook to include telegram_notify.py:
   Add telegram_notify.py command to the existing Notification hook array after the "say" command